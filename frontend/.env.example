# API Security
# Must match the API_KEY in backend .env
# Leave empty if backend has no API_KEY set (dev mode)
NEXT_PUBLIC_API_KEY=your-secret-api-key-here

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL (for development)
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Production URLs (set these in Vercel)
# NEXT_PUBLIC_API_KEY=your-production-api-key
# NEXT_PUBLIC_API_URL=https://your-backend-url.railway.app
# NEXT_PUBLIC_WS_URL=wss://your-backend-url.railway.app
